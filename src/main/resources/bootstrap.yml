spring:
  application:
    name: logiscdweb-data
  profiles:
    active:
      - dev
  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 8081
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka